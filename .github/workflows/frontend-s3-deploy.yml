name: Frontend S3 Deploy

on:
  push:
    branches:
      - Level--300

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/setup-node@v1
  with:
    node-version: 12
    -run: npm ci
    -run:npm run build

      
