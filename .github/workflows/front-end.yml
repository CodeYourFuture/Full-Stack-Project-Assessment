name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      working-directory: ./client
      uses: actions/checkout@v3

    - name: Install dependencies
      run: npm install 

    - name: Build app
      run: npm run build 