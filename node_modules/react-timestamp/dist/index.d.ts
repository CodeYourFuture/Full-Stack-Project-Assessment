/// <reference types="react" />
import { FormatOptions } from "./util";
interface TimestampProps {
    className?: string;
    style?: object;
    component?: any;
    date: Date | number | string;
    relative?: boolean;
    relativeTo?: Date | number | string;
    options?: FormatOptions;
    autoUpdate?: boolean;
}
export default function Timestamp({ className, style, component, date, relative, relativeTo, options, autoUpdate, }: TimestampProps): JSX.Element;
export {};
