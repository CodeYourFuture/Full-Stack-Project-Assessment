import NavigationItems from '../navigationItems/NavigationItems';

const Toolbar = (props) => {
  return (
    <header>
      <nav>
        <NavigationItems />
      </nav>
    </header>
  );
};

export default Toolbar;
