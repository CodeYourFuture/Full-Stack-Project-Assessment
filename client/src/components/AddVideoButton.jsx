import React from "react";
import Button from "react-bootstrap/Button";

function AddVideoButton({ displayForm }) {
  return <Button onClick={displayForm}>Add Video</Button>;
}

export default AddVideoButton;
